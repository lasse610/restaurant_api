{
  "name": "restaurant_api",
  "version": "1.0.0",
  "description": "api for fetching restaurants in json format. Queries can have params for searching by name and location.",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll --verbose"
  },
  "keywords": [
    "wolt",
    "restaurant_api"
  ],
  "author": "Lasse Tammela",
  "license": "ISC",
  "dependencies": {
    "config": "^3.2.5",
    "express": "^4.17.1",
    "node": "^13.7.0"
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "jshint": "^2.11.0",
    "supertest": "^4.0.2"
  }
}
